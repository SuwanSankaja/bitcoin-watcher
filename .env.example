# Bitcoin Watcher Environment Variables
# Copy this file to .env and fill in your values

# AWS API Gateway URL
API_BASE_URL=https://your-api-gateway-url.execute-api.us-east-1.amazonaws.com/prod

# Firebase (android/app/google-services.json and ios/Runner/GoogleService-Info.plist)
# These files should be downloaded from Firebase Console and placed in the respective directories

# MongoDB Connection String (stored in AWS Parameter Store)
# /bitcoin-watcher/mongodb-uri

# Firebase Admin SDK (stored in AWS Secrets Manager)
# bitcoin-watcher-firebase-creds
